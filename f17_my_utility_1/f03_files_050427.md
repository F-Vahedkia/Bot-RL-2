# Python Module Summary

## File_1: `f03_features/feature_C_engine_4.py`

### Top-level Functions

_None_

### Top-level Classes

#### class `FeatureEngine`

- `__init__(self, config: Dict[str, Any])`
- `execute(self, dataset: MTFDataset, specs: List[str], mode: str = 'train') -> MTFDataset`
- `_apply_spec(self, dataset: MTFDataset, ps: ParsedSpec, mode: str) -> MTFDataset`
- `_call_batch(self, fn, dataset: MTFDataset, ps: ParsedSpec) -> MTFDataset`
- `_apply_live(self, spec: IndicatorSpec, df: pd.DataFrame, ps: ParsedSpec) -> pd.DataFrame`
- `_update_live(self, obj: Any, row: pd.Series, spec: IndicatorSpec, ps: ParsedSpec)`
- `_build_live_instance(self, spec, ps)`
- `_merge(self, df: pd.DataFrame, out: pd.DataFrame) -> pd.DataFrame`
- `_attach_live_output(self, df: pd.DataFrame, ps: ParsedSpec, spec: IndicatorSpec, outputs)`
- `_build_live_column_name(self, output_name, ps)`
- `_normalize_output(self, spec, output)`
- `_validate_contract(self, spec, df)`
- `_build_graph(self, parsed_specs)`
- `_resolve_order(self, graph)`
- `process_live_data(self, dataset: MTFDataset) -> Optional[MTFDataset]`

## File_2: `f03_features/feature_B_store.py`

### Top-level Functions

- `_normalize_column(col: str) -> str`
- `_parse_column(col: str) -> Optional[Tuple[str, str, str, str]]`
- `align_to_base(dataset: MTFDataset, *, candles: list[str]) -> pd.DataFrame`
- `build_feature_store(dataset: MTFDataset, features: MTFDataset, out_dir: str | Path, name: str, fmt: str = 'parquet', config = None) -> Dict[str, Dict[str, str]]`

### Top-level Classes

#### class `FeatureMeta`

_No methods_

#### class `FeatureStoreV2`

- `__init__(self, config = None)`
- `extract_metadata(self, dataset: MTFDataset) -> pd.DataFrame`
- `_rename_columns_old1(self, df: pd.DataFrame, symbol: str, timeframe: str) -> pd.DataFrame`
- `_rename_columns_old2(self, df: pd.DataFrame, symbol: str, timeframe: str) -> pd.DataFrame`
- `_rename_columns(self, df: pd.DataFrame, symbol: str, timeframe: str, mode: str = 'none') -> pd.DataFrame`
- `build(self, dataset: MTFDataset, features: MTFDataset) -> MTFDataset`
- `extract_metadata(self, dataset: MTFDataset) -> Dict[str, pd.DataFrame]`
- `save(self, dataset: MTFDataset, metadata: Dict[str, pd.DataFrame], out_dir: str | Path, name: str, fmt: str = 'parquet') -> Dict[str, Dict[str, str]]`

## File_3: `f03_features/observation_B_builder.py`

### Top-level Functions

_None_

### Top-level Classes

#### class `ObservationBuilder`

- `__init__(self, config: Dict[str, Any])`
- `build(self, dataset: MTFDataset, graph: FeatureGraph) -> pd.DataFrame`
- `build_numpy(self, dataset: MTFDataset, graph: FeatureGraph)`

## File_4: `f03_features/feature_B_graph.py`

### Top-level Functions

_None_

### Top-level Classes

#### class `FeatureNode`

_No methods_

#### class `FeatureGraph`

- `__init__(self, specs: List[str])`
- `_build(self)`
- `get_by_timeframe(self, tf: str) -> List[FeatureNode]`
- `get_by_name(self, name: str) -> List[FeatureNode]`
- `all_nodes(self) -> List[FeatureNode]`

## File_5: `f02_data/mtf_dataset.py`

### Top-level Functions

_None_

### Top-level Classes

#### class `MTFDataset`

- `add(self, timeframe: str, df: pd.DataFrame) -> None`
- `get(self, timeframe: str) -> pd.DataFrame`
- `__getitem__(self, timeframe: str) -> pd.DataFrame`
- `timeframes(self) -> List[str]`
- `copy(self) -> 'MTFDataset'`
- `apply(self, func: Callable[..., pd.DataFrame], *args: Any, **kwargs: Any) -> None`
- `apply_each(self, func: Callable[..., pd.DataFrame], *args: Any, **kwargs: Any) -> 'MTFDataset'`
- `replace(self, timeframe: str, df: pd.DataFrame) -> None`

## File_6: `f02_data/data_handler_F_2_3_n.py`

### Top-level Functions

- `_read_raw_df(path: Path, mode: Literal['number', 'time'] = 'number', start_lastrows: Optional[int] = None, end_lastrows: Optional[int] = None, start_time: Optional[Union[str, datetime]] = None, end_time: Optional[Union[str, datetime]] = None, columns: Optional[List[str]] = None, broker_timezone: str = 'UTC') -> pd.DataFrame`
- `_get_range_by_timeframe(timeframe: str, from_last_n: int, to_last_n: int, base_time: Optional[Union[str, pd.Timestamp]] = None) -> Tuple[pd.Timestamp, pd.Timestamp]`
- `ensure_timezone_aware(dt, tz = 'UTC')`
- `_parse_hhmm_in_utc(s: str) -> time`
- `_in_utc_range(t: datetime, start: time, end: time) -> bool`
- `_add_session_flags(df: pd.DataFrame, cfg: Dict[str, Any]) -> pd.DataFrame`
- `_merge_on_base(base_df: pd.DataFrame, other_df: pd.DataFrame, prefix: str) -> pd.DataFrame`
- `_check_ohlc(df: pd.DataFrame, prefix: str) -> None`
- `_add_time_features_to_df(df: pd.DataFrame, cfg: Dict[str, Any]) -> pd.DataFrame`
- `_add_qc_flags_to_df(df: pd.DataFrame, cfg: Dict[str, Any], base_tf: str, symbol: str) -> pd.DataFrame`
- `_finalize_dataframe(df: pd.DataFrame, cfg: Dict[str, Any], base_tf: str, symbol: str, tfs: List[str]) -> pd.DataFrame`
- `_setup_logging(level: str = 'INFO') -> None`
- `_parse_args()`
- `main() -> int`

### Top-level Classes

#### class `BuildParams_old`

_No methods_

#### class `BuildParams`

- `__init__(self, symbol: str, base_tf: str, timeframes: Optional[List[str]], selected_tf: Optional[str] = None, load_format: Literal['csv', 'parquet'] = 'parquet', mode: Literal['number', 'time', 'periods'] = 'number', start_lastrows: int = None, end_lastrows: int = None, start_time: datetime = None, end_time: datetime = None, period_size: str = None, from_last_n: int = None, to_last_n: int = None, base_time: datetime = None) -> None`

#### class `DataHandler`

- `__init__(self, cfg: Dict[str, Any], symbol: str, event_bus: Optional[EventBus] = None) -> None`
- `_load_raw(self, params: BuildParams) -> pd.DataFrame`
- `build(self, params: BuildParams) -> pd.DataFrame`
- `save(self, dataset: MTFDataset, symbol: str, base_tf: str, fmt: Optional[str] = None)`
- `subscribe_to_event_bus(self, event_bus: EventBus) -> None`
- `start_consuming2(self) -> None`
- `on_new_candle2(self, event: Dict[str, Any]) -> None`
- `update_live2(self, symbol: str, timeframe: str, all_dfs: Dict[str, Any]) -> Dict[str, pd.DataFrame]`
- `_update_cache(self, symbol: str, timeframe: str, all_dfs: Dict[str, pd.DataFrame]) -> None`
- `get_latest_dataset(self) -> Optional[MTFDataset]`
- `set_data_callback(self, callback)`
- `_notify_new_data(self, dataset: MTFDataset)`
- `stop_consuming(self) -> None`

